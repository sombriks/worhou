mixin key-val(key, val,attr={})
    .is-flex.is-justify-content-space-between.is-align-items-center.is-gap-2.m-2&attributes(attr)
        .is-bold= key + ': '
        .is-flex-grow-1
        .is-italic= val ? val : '-'
