mixin navbar-item(label,link,icon)
    a.navbar-item(href=link x-data="{}"
        :class=`{'is-active': window.location.href.includes('${link}')}`)
        span.icon-text
            span.icon
                i(class=icon)
            span=label
