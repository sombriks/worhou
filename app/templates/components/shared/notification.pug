mixin notification(cls='is-info')
    .notification.is-textAlign-center.py-6(x-data="{}" class=cls)
        button.delete(
            aria-label="close"
            @click="$el.closest('.notification').remove()")
        if block
            block
        else
            i -
