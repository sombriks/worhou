mixin modal
    .modal.is-active(x-data="{}")
        .modal-background
        .modal-content
            if block
                block
        button.modal-close.is-large(
            aria-label="close"
            @click="$el.closest('.modal').remove()")
