include ../shared/message

mixin me()
    +message("Profile")
        p Login details
        .field.is-grouped.is-grouped-right(x-data="{}")
            button.button.is-warning(
                @click="new WorHou().logout()") Log out
