extends ../layouts/main
include ../components/shared/message

block head
    title Timelog
block content
    .timelog-content(
        hx-trigger="load",
        hx-swap="outerHTML",
        hx-get=`${base}/timelog/today`).
        | loading...
