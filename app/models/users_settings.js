import {Base} from './_base.js';

export const UsersSettings = Object.freeze({
  ...Base,
  _name: 'users_settings',
  users_id: 'users_id',
  job_title: 'job_title',
  shift_size: 'shift_size',
  hours_per_day: 'hours_per_day',
  hours_per_week: 'hours_per_week',
  hours_per_month: 'hours_per_month',
});
