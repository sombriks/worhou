export const page = async (request, res) => res.view('pages/worksheet');

// Ver os registros agrupados por dia
// somar o total de horas do dia em cada linha
// uns filtros pra ver a semana, o mes, o ano
// um totalizador para a lista
